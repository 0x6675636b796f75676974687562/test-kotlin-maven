name: 'diKTat Check Workflow'

on:
  pull_request:
  push:
  workflow_dispatch:

jobs:
  diktat_check:
    name: 'diKTat Check Job'
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        os: [ ubuntu-latest, windows-latest, macos-latest ]

    steps:
      - name: 'diKTat Check using a GitHub Action'
        uses: 0x6675636b796f75676974687562/benedikt@master
